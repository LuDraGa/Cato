# Cato

Flutter app — Android only.

## Platform

- Target: Android only
- Removed platform dirs (windows, ios, macos, linux, web) to save space
- To re-add a platform later: `flutter create --platforms=<platform> .`

## Build

- Build artifacts (`build/`) are gitignored and can be regenerated with `flutter build`
